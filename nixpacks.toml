[phases.setup]
nixPkgs = ['php82', 'php82Extensions.gd', 'php82Extensions.pdo_mysql', 'nodejs']

[phases.install]
cmds = ['composer install --optimize-autoloader --no-dev --no-interaction']

[phases.build]
cmds = [
    "npm run build",
    "php artisan config:cache"
]

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=$PORT'
